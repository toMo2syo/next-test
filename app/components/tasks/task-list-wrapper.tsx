// import { fetchTasks } from '@/app/lib/actions'
// import { db } from '@/app/lib/db'
import React from 'react'

export default async function TaskListWrapper() {
    //  const tasks = await fetchTasks()
    return (
        <div>task-list-wrapper</div>
    )
}
